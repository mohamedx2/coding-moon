# ═══════════════════════════════════════════════
# SmartEdu AI – Environment Configuration
# Copy to .env and fill in your values
# ═══════════════════════════════════════════════

# ── Application ──
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# ── Database ──
DATABASE_URL=postgresql+asyncpg://smartedu:smartedu_pass@localhost:5432/smartedu

# ── Redis ──
REDIS_URL=redis://localhost:6379/0

# ── JWT Authentication ──
JWT_SECRET_KEY=change-this-to-a-random-256-bit-secret
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ── OpenAI ──
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_MAX_TOKENS=4096

# ── OAuth (Optional) ──
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=

# ── Rate Limiting ──
RATE_LIMIT_GENERAL=10/second
RATE_LIMIT_AUTH=5/minute
RATE_LIMIT_AI=2/minute

# ── AI Quotas ──
AI_QUOTA_STUDENT_DAILY=50
AI_QUOTA_TEACHER_DAILY=500

# ── CORS ──
CORS_ORIGINS=http://localhost:3000

# ── Frontend ──
NEXT_PUBLIC_API_URL=http://localhost:8000

# ── Monitoring (Optional) ──
SENTRY_DSN=
